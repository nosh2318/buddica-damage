-- ============================================================
-- BUDDICA 車両傷管理システム セットアップSQL（高松店）
-- Supabase SQL Editor に貼り付けて「Run」を押してください
-- ============================================================

-- schema.sql を先に実行してください
-- このファイルは追加のセットアップ用です

-- ============================================================
-- 確認クエリ（実行後にこれで確認）
-- ============================================================
SELECT id, store, plate, model, status, current_damages
FROM vehicle_twins
WHERE store = 'takamatsu'
ORDER BY id;
